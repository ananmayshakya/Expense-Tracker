# Screenshots

Add PNGs here, referenced by the README:

- `dashboard.png` — the dashboard (stat cards + charts)
- `transactions.png` — the expenses/transactions page
- `budgets.png` — the budgets page with progress bars
- `dark-mode.png` — any page with dark mode enabled
